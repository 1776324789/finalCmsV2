import express from 'express'
import cors from 'cors'
import path from 'path'
import fs from 'fs'

export const loginServer = (app) => {

    app.get('/api/list/', (req, res) => {
        res.json(data)
    })


    app.get('/api/list/', (req, res) => {
        res.json(data)
    })
}

